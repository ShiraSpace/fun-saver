export const TOTAL_CHIP_TEST_IDS = {
  chip: 'total-chip',
  amount: 'total-chip-amount',
} as const;

export const TOTAL_CHIP_COPY = {
  label: 'סך הכל',
} as const;

export const TOTAL_CHIP_STYLE = {
  radius: 14,
  paddingY: 5,
  paddingX: 12,
  borderWidth: 1.5,
  labelFontSize: 8,
  labelWeight: 600,
  labelLetterSpacing: 0.3,
  lineHeight: 1.15,
} as const;
