import { CHART_HEIGHT } from '../constants';

export const DAY_TICK_ABOVE_BOTTOM = 3;
export const DAY_TICK_Y = CHART_HEIGHT - DAY_TICK_ABOVE_BOTTOM;

export const DAY_AXIS_TEST_IDS = {
  tick: 'day-axis-tick',
} as const;
